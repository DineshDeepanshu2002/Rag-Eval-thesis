# Exact pinned versions (verified against the environment used to produce all results).
# Tested with Python 3.11+.
pyyaml==6.0.3
numpy==2.5.1
scipy==1.18.0
pandas==3.0.5
beir==2.2.0
sentence-transformers==5.6.1
openai==2.52.0
ragas==0.4.3
datasets==5.0.1
langchain-openai==1.4.1
